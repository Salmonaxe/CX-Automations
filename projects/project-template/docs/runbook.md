# Runbook

## Local Setup

1. Create and activate a virtual environment.
2. Install dev dependencies (for example, `pytest`).
3. Copy `.env.example` to `.env` and set values.

## Run

```powershell
python src/main.py
```

## Test

```powershell
python -m pytest
```
